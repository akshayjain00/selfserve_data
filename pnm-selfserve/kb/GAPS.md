# GAPS.md — open questions, conflicts, and uncovered surface

`PNM-G-###` rows, append-only. Adding/closing rules: [CONTRIBUTING.md](./CONTRIBUTING.md) §11.
Seeded 2026-08-26 from the re-cut of `pnm-gem-knowledge.md` into row-addressed form.

**A gap is not a failure — it is a known unknown with an owner and a next action.** The dangerous
state is a fact that *looks* verified and isn't. Every row below has a `next_action` specific enough
to execute.

**Status:** `OPEN` · `BLOCKED` (needs a person or a decision) · `CLOSED` (with date + resolving id).

---

## A. Provenance and citation integrity

| id | gap | next_action | status |
|---|---|---|---|
| ~~**PNM-G-002**~~ | ~~Two PnM sources are untracked in git and therefore uncitable at a SHA — `iteration-3-p80-orderedits-spec.md` and `coverage-map/metric-coverage.json`~~ | — | **CLOSED 2026-08-26** — **the premise was already false when this row was written.** Both files are tracked on `main`: the spec since **2026-07-19** (`83011b2`), **38 days *before*** the KB was built at `3d535b6` (2026-08-26), and the coverage map since **2026-08-26** (`03f1653`). Citations re-pointed `local:` → `repo@<sha>:` at three call sites — `PNM-S-032`, `PNM-S-037` and **`PNM-B-062`**, which this row's `next_action` did not name. ⚠ **The `§4.1` ceiling lifts; the `unverified` grade does not** — `§5` independently caps a design document and a CSV projection, so both stay `unverified` for a different reason. Two live traps recorded at `PNM-S-032`: the **Desktop clone holds an older 2026-07-12 draft** that must never be copied over the tracked board-reviewed revision, and this row previously *instructed* exactly that copy |
| **PNM-G-003** | **PnM's strongest evidentiary rung cannot itself produce a `verified` row.** The MBR automation is out-of-repo, so it is `local:`-capped. The KB works around this by citing `sqlgen.py`, its in-repo mirror — but the workaround's validity rests entirely on the reconciliation staying true. ⚠ **Partial evidence 2026-09-04**: a DEV-ingestion draft of the automation (`PNM-S-060`) is now tracked at a SHA, extending the "in-repo mirror" workaround from 4 sections to all 14 — but it is a draft supplied in-session, not `queries.py` itself, so the core ask (version control the actual production file) is unmet | Place the automation under version control, or record a hash of it per reconciliation run so the mirror claim is checkable | **BLOCKED** — owner |
| **PNM-G-004** | **The reconciliation covers a bounded slice, and the KB leans on it everywhere.** `V3` covers 2026-05 for leads/orders/derived/tpo; `V4` covers p80 (8 baseline months) and order_edits (Mar/Apr/May). Several metrics were **never individually reconciled** — the three channel conversions, all three order-mix metrics, and all ten TPO stage metrics. ⚠ **Narrowed 2026-09-04, not closed — true reconciliation is structurally blocked, same root cause as `PNM-G-003`/`PNM-G-006`.** The automation is `local:`-capped and out-of-repo; without it, nothing beyond *self-consistency* is checkable. What self-consistency evidence a live re-run for **2026-06** actually shows: (1) **channel conversions and order-mix are not independent risk** — `conversion_app` etc. are Python-computed ratios of `orders_x`/`leads_x`, and those channel counts were the exact figures `V3` matched to the automation for 2026-05; a ratio of two exactly-matched numbers is exactly right *if* the automation defines the ratio the same way (unverified, but D7(b) documents this as the deliberate design). (2) **The 5 TPO stage buckets (`pre_trip`+`trip_shift`+`pickup`+`completed`+`cancelled`) sum to exactly `tpo_overall` for 2026-06** (0.4127+0.2912+0.0739+0.1362+0.0846 = 0.9986) — a clean partition, no ticket double-counted or dropped across stages. (3) Leads/orders channel splits still sum to their overall for 2026-06 (310,928 = 217,312+10,946+63,863+18,807; 49,472 = 40,028+1,374+6,915+1,155), same invariant `V3` checked for 2026-05. **None of this is a match against the automation's own numbers** — only V3's single 2026-05 snapshot has that. The customer-subcategory legs (`_cust` suffixes, 5 of the 10 TPO stage ids) remain genuinely unchecked even for self-consistency beyond "≤ their parent" | Extend the differential reconciliation to more months and to the per-channel and per-stage metrics **once the automation is accessible** (`PNM-G-003`/`PNM-G-006`). Until then, self-consistency checks like the above are the most this KB can produce — record which months got which level of check, and do not conflate "internally consistent" with "reconciled" | **OPEN** |
| **PNM-G-006** | **No freshness check exists for the MBR automation.** It has no SHA readable offline and no `updated_at`. If the owner edits it, this KB has no way to notice | Same action as `PNM-G-003` | **BLOCKED** — owner |
| ~~**PNM-G-007**~~ | ~~`PROD_ELDORIA.MART.PNM_EXPERIENCE` was "still under active construction," with a history of mid-project schema growth — a standing schema-drift risk, re-verified twice (2026-07-29, 2026-09-04) with zero drift found either time.~~ | — | **CLOSED 2026-09-07 by owner ruling** (`owner-ruling:2026-09-07`). **`PNM_EXPERIENCE` is no longer under active construction — it is in its final shape and form.** This removes the root cause option (1) from the 2026-09-04 note (`DECISION_LOG:D15`) never reached: NI_PNM has settled the mart, not just "might later." Option (2) (migrate away) was already ruled out the same session `PNM_EXPERIENCE` was confirmed fixed as the source for `p80_durations`/`order_edits`. With the mart declared final, the recurring-check framing this gap carried (`PNM-G-004`-style, "never fully closable") no longer applies — there is nothing further to wait on. Evidence: `DECISION_LOG:D20` |
| ~~**PNM-G-008**~~ | ~~No Metabase card carries a staleness fingerprint in this KB.~~ | — | **CLOSED 2026-09-04** — fingerprinted all three via the Data Catalog's `list_metabase` (equivalent to `get_card`, no Metabase auth needed): **#30311** `updated_at` 2026-03-30, **#47576** `updated_at` 2026-05-08, **#37409** `updated_at` 2025-12-08. Recorded on `PNM-S-010`/`PNM-S-011`/`PNM-S-059`. Notably, **#37409 has not changed since 9 months before the 2026-09-04 owner ruling that adopted it** — the ruling was made against a settled definition, not a moving one. ⚠ **A fingerprint is a point-in-time check, not a standing alarm** — nothing re-runs this automatically; a future session must re-`get_card` to detect drift, same limitation as `PNM-G-007`'s schema pre-flight |


## B. Code behaviour that no document ratifies

*Read out of `sqlgen.py` / `metrics_registry.py`, so `verified` as **what the code does** — but
nothing states that it is **what the business intends**. That gap is the point of these rows.*

| id | gap | next_action | status |
|---|---|---|---|
| ~~**PNM-G-011**~~ | ~~The channel `CASE` falls through to `ELSE 'Mobile Website'`.~~ | — | **CLOSED 2026-09-04 — sized, immaterial.** Ran the actual `leads_base`/`orders_base_raw` CASE logic live across 4 months (2026-03, 05, 06, 08 — 1,230,561 leads, 189,893 orders): **leads: 0 hits, 0%** — every lead's `(source, source_details)` combination matched one of the first four `WHEN`s; `source` was never NULL and `source = 0` always carried a `Desktop Website`/`Mobile Website` `source_details` tag, so the originally-feared "unbranched Website source" case does not occur in live data. **orders: 28 hits out of 189,893 (0.015%)** — a real but tiny rate, and via a **different mechanism than hypothesized**: all 28 have `source IS NULL`, from the orders CTE's longer join chain (`order → sr_id → opportunity → opportunity dim`) failing to find a matching opportunity row, not from an unbranched `source = 0`. **Immaterial at 0.015%** — no fix made, no owner escalation needed at this size. If this ever needs owner attention it's now for a different, smaller reason than the row originally described |
| ~~**PNM-G-012**~~ | ~~Null-package orders are kept as non-Nano.~~ | — | **CLOSED 2026-09-04.** Sized live: **zero** orders with a blank `package_name` across 4 months / 220,402 orders — the ambiguity the filter's `OR package_name IS NULL` clause resolves never actually occurs in current data. **Ruled by the KB owner's session stand-in 2026-09-04: keep the rule as-is** — "whatever is not `ILIKE '%Nano%'` is PnM, until current context changes." Not an owner-of-record `DECISION_LOG` ruling; if the null population ever becomes non-zero, re-open and get a real `D<n>`. ⚠ **Related question raised and closed the same day**: a second query appeared to identify Nano/Labour orders via CRN prefix (`LBR%`) rather than `package_name` — checked live, disproven (`PNM-G-061`): Nano-package orders carry no distinguishing CRN pattern, so `package_name` remains the only signal and this row's "zero nulls" finding stands as the complete picture |
| **PNM-G-013** | **The order dedup tiebreak is `ORDER BY fpo.opp_id DESC NULLS LAST`.** The registry calls it "deterministic" — which is not the same as correct. Nothing states why the **highest** `opp_id` is the right opportunity to inherit channel from. 🔴 **Measured 2026-09-04 — this is material, not a technicality.** Across 4 months (2026-03/05/06/08, 189,893 orders): **44,255 orders (23.3%) have more than one candidate opportunity** in the join window, and of those, **14,763 (7.8% of ALL orders) would land in a different channel bucket depending which candidate opportunity wins the tiebreak.** That is roughly **1 in 13 orders in the entire catalog whose `orders_app`/`orders_desktop`/`orders_mobile`/`orders_others` bucket is decided by this arbitrary rule, not by the order's own data.** `V3` shows `DESC` (highest `opp_id`) reproduces the automation's 2026-05 numbers exactly, so the current rule is at least **verified-correct as a mirror** — but "matches the automation" and "is the right business rule" are different claims, and at this size the difference is not academic. ✅ **Corroborated 2026-09-04** — a second, separately-maintained "business conversion" query (supplied in-session by the KB owner, no file/card citation given, so this is evidence, not a sourced fact) carries the **identical** `QUALIFY ROW_NUMBER() OVER (PARTITION BY o.order_id ORDER BY fpo.opp_id DESC NULLS LAST) = 1`. **This is real, consistently-applied logic, not a one-off implementation default** — but consistency across two reports still isn't a stated business reason for *why* highest-`opp_id` is correct, and the 7.8%-of-orders impact is unchanged | **Escalate to the owner now** — this is large enough to change published channel-mix conclusions. Ask specifically: should channel be inherited from the *most recent* opportunity, the *first* one, or something else entirely — "highest `opp_id`" was never a stated business rule, only a shared implementation default that happens to match the automation | **OPEN** |
| ~~**PNM-G-014**~~ | ~~Two silent population filters nobody has sized.~~ | — | **CLOSED 2026-09-04 — both sized, both immaterial.** (a) The `FACT_PNM_ORDERS → PNM_CUSTOMERS` inner join: **0 orders dropped** across 4 months / 220,402 orders — every order's mobile has a matching customer row. (b) TPO's `COALESCE(hst.shifting_type, c.shifting_type)` fallback, checked across 4 months / 170,723 tickets: `hst.shifting_type` is **never NULL** (0 tickets), so the fallback never actually fires — it's dead code in practice; and on the 170,723 tickets where both values exist, they **disagree only 8 times (0.005%)**. No caveat added to `PNM-M-002`/`PNM-M-009` — the size doesn't warrant one |
| ~~**PNM-G-015**~~ | ~~The detractor filter is `COALESCE(raised_by,'') != 'Detractor'` — exact equality. iteration-1 records the pipeline's filter as `LOWER(raised_by) LIKE '%detractor%'`. The two disagree on case and on substring: a value like `Detractor-Customer` or `detractor` is excluded by one and kept by the other~~ | — | **CLOSED 2026-09-04** — `GROUP BY raised_by` on all of `PROD_CURATED.SFMS_PUBLIC.HS_TICKETS` returns exactly **6** distinct values, no `NULL`s: `Customer` (1,966,158), `Vendor-Owner` (619,134), `Vendor-Supervisor` (331,046), `Porter Support` (134,293), `Detractor` (76,653), `Chat` (9,168). `'Detractor'` is the only case/substring variant that occurs, so `= 'Detractor'` and `LIKE '%detractor%'` select the identical 76,653 rows today. **Agreement is a fact about current data, not a proof of equivalence** — a future `Detractor-Customer` value would silently re-diverge the two filters |
| ~~**PNM-G-016**~~ | ~~TPO ratios are `ROUND(…, 4)` while the automation emits 2dp, and `V3` asserts they "round identically" without demonstrating it.~~ | — | **CLOSED 2026-09-04.** Checked directly: "round the raw ratio straight to 2dp" vs "round to 4dp, then round that result to 2dp" — compared across **all 12 TPO ratio metrics × 11 months (2025-10 to 2026-08) = 132 values, zero mismatches.** 10 of the 132 sat close to a rounding cutoff (within 0.05 of a boundary); none of those flipped either. The "rounds identically" claim in `V3` was true for the 2 metrics/1 month it originally checked, and now holds empirically for all 12 metrics across 11 months |
| ~~**PNM-G-017**~~ | ~~`UNSUPPORTED_TERMS` hardcodes 40 terms including 15 city names, sourced to a code comment that cannot be checked offline.~~ | — | **CLOSED 2026-09-04 — watch `ludhiana`, same pattern as other rows in this pass.** Checked live: every city with real order volume (14 cities, `PNM_ALLOCATION.PICKUP_CITY_NAME`, trailing 12 months) is already covered by the guard's 15 terms — no current gap. One candidate risk found: `DIM_GEO_REGIONS.IS_HOUSE_SHIFTING_ENABLED = TRUE` flags **`ludhiana`**, not in the guard list, currently **zero PnM order volume**. Deriving the list from `DIM_GEO_REGIONS` live instead of hardcoding it would close this permanently, but that's a code change to `metrics_registry.py`, out of scope for a KB-only pass. **If `ludhiana` ever shows PnM order volume, `UNSUPPORTED_TERMS` needs the term added before any question naming it can be trusted to refuse rather than silently answer PnM-wide** |
| ~~**PNM-G-041**~~ | ~~"Only the owner promotes readiness" is stated only in a docstring, resting on registry prose (rung 7), not an owner ruling.~~ | — | **CLOSED 2026-09-07 by owner ruling** (`owner-ruling:2026-09-07`, `DECISION_LOG:D18`). Ratified as a real rule at rung 1: no AI session, and no analysis result however strong, may promote a metric to `stakeholder_ready` — only the owner does, explicitly. `PNM-B-042` upgraded `unverified` → `verified`. Nothing changes in practice — every section has always been `prototype_only` — this closes the gap between how the KB has behaved and what it can cite as the reason why |
| ~~**PNM-G-042**~~ | ~~~18 `data-model.md` facts rest on a live schema read this KB did not perform.~~ | — | **CLOSED 2026-09-04** — re-ran `INFORMATION_SCHEMA.COLUMNS` first-hand for all 11 catalog tables (`PNM-T-001`–`011`, incl. `DIM_GEO_REGIONS`), no owner go-ahead needed (read, not write, same finding as `PNM-G-007`). **Every column count matched the inherited 2026-07-29 figures exactly — zero drift in 39 days.** The load-bearing absence claim, `PNM-T-032` (`HS_TICKETS` has no `ORDER_ID`), was checked column-by-column, not just by count: confirmed absent (the table has `HS_ORDER_ID` and `SF_ORDER_ID`, neither is `ORDER_ID`). All 11 rows in `data-model.md`'s "Tables the catalog reads" upgraded `unverified` → `verified` at `live:INFORMATION_SCHEMA@2026-09-04` |
| ~~**PNM-G-043**~~ | ~~"`ota`'s six columns exist in no table" is only partly evidenced. The registry's evidence checked two of the six by name (`scheduled_pickup_ts`, `vendor_arrived_ts`); the four coordinate columns are covered only by "the staging table never materialises them"~~ | — | **CLOSED 2026-09-04 — superseded, not resolved as originally framed.** The six-column claim itself was never re-checked column-by-column (the four coordinate names remain unverified by name). Instead, `PNM-G-024`'s owner ruling adopted **Card #37409**, which builds OTA from entirely different, confirmed-existing columns (`PNM_APPLICATION_SR_LOCATION_DETAILS.LOCATION`, `PNM_APPLICATION_SUPERVISOR_ACTIONS.LOCATION`/`EVENT_TS_IST`) — so whether the original six columns exist is now moot: the `ota` section is **built** or a different sourcing question. The old blocked_reason evidence is preserved (not deleted) in `metrics_registry.py`'s git history and in this row |
| ~~**PNM-G-018**~~ | ~~`conversion_others` does not exist, though `leads_others` and `orders_others` both do.~~ | — | **CLOSED 2026-09-04 — a second, independent implementation confirms this is deliberate, not an oversight.** Already had one signal: the Argus DD carries Conversion Overall/App/Desktop/Mobile and no "Conversion — Others" row. Now a second: the "business conversion" query (`PNM-G-013`'s corroborating source) computes `generic_leads`, `generic_orders`, and even `generic_order_pct` (the order-mix %) — but its final `SELECT` never builds a `generic_conv_pct`. **Two independently-built reports both stop short of a conversion % for the Others/Generic bucket** — the same omission, in two places nobody coordinated. Recorded on `PNM-M-005` in `metrics.md` |
| ~~**PNM-G-019**~~ | ~~`month_bounds()` still returns an unused `month_start_prev`, "retained for API stability". It is the last trace of the two-month attribution window, whose removal silently voided iteration-1 §2b#5's entire undercount analysis — which remains unannotated in that file~~ | — | **CLOSED 2026-09-04** — confirmed **no caller anywhere reads `month_start_prev`**: all 7 call sites (`sqlgen.py` ×6, `run_tests.py` ×1, `ask.py` ×1/×1-ignored) discard it as `ms, _ = month_bounds(...)`. The docstring's own claim that "callers still reference it" is false. **`month_start_prev` is dead code left over from an old approach this system no longer follows**: iteration-1's legacy 5-file pipeline filtered staging tables to a **two-month window** (current + previous month, lines 27–30) and, as a direct consequence, suffered a documented channel-attribution undercount near window boundaries (line 53). `D3`/`D5` replaced that pipeline with the shipped single-month query design — the two-month window, and the undercount bug that came with it, no longer exist in the running system. `month_start_prev` is the sole surviving trace. **Resolution: annotate only, don't touch shipped code.** `sqlgen.py` is left as-is (harmless dead value, zero blast radius); per the KB's standing convention (`PNM-S-030`, Group C intro above) iteration-1's own text is never edited — the superseding note lives in `PNM-S-030` instead, extended to name this row |
| ~~**PNM-G-020**~~ | ~~The registry's `p80_durations.month_basis` still reads "calendar month of `o_completed_ts`" — a legacy raw-table column `D3` established does not exist, and `D8` set the basis to `SHIFTING_TS_IST`~~ | — | **CLOSED 2026-09-04** — **the premise was already false when this row was written.** `metrics_registry.py`'s `p80_durations.month_basis` reads `"calendar month of SHIFTING_TS_IST (shifting/execution month)"` (`repo@851886f:pnm-selfserve/selfserve_nlq/metrics_registry.py:225`), fixed in commit `851886f` (2026-07-19, "Implement p80_durations + order_edits"), carrying its own `# [board-fix] corrected from the stale stub value "o_completed_ts"` comment — **38 days before** this KB was seeded (2026-08-26). The stale `"calendar month of o_completed_ts"` string that does exist in the file is on the **`ota`** section (line 201), already covered separately by `PNM-G-021` |
| ~~**PNM-G-021**~~ | ~~The registry's `ota.base_population` still reads "completed (`status=2`)" — `status` is the legacy numeric column that does not exist on the re-pointed tables~~ | — | **CLOSED 2026-09-04** — `ota` unblocked (`PNM-G-024`) and its `base_population` rewritten as part of the build: `"completed (dim_pnm_orders.order_status='completed'), intra-city (shifting_type='intra_city'), NON-Nano (package_name NOT ILIKE 'Nano%')..."`, matching Card #37409's real predicate. Old string replaced, not appended, per the same convention used for `D8`'s `order_edits` correction |
| **PNM-G-022** | **`CONFIG_WIDE_FLAGS` still carries "Canonical methodology: Metabase card #30311" verbatim as a live open flag**, though `D6` ruled #30311 non-authoritative for PnM | Correct the comment **at source** in `config.py` (owner-held, not in this repo), then retire the flag | **BLOCKED** — owner |

## C. iteration-1 vs the owner rulings that superseded it

> `D5` states it "corrected **six** things my initial re-point had wrong". Those corrections landed in
> `sqlgen.py`; **iteration-1 was never annotated.** A reader who opens it gets wrong answers with no
> warning. Recorded here, **not silently resolved** — `PNM-S-030` carries the standing warning.

| id | question | iteration-1 says | `D4`/`D5` + shipped SQL say | next_action | status |
|---|---|---|---|---|---|
| ~~**PNM-G-030**~~ | ~~Nano in orders~~ | ~~INCLUDED~~ | ~~EXCLUDED (`package_name NOT ILIKE 'Nano%'`)~~ | — | **CLOSED 2026-09-04** — re-verified against current `sqlgen.py`: `CTE_ORDERS` reads `AND (dord.package_name NOT ILIKE 'Nano%' OR dord.package_name IS NULL)`, confirming EXCLUDED. Annotation requirement satisfied by the pre-existing standing warning at `PNM-S-030` ("six of its definitions are actively wrong — `PNM-G-030`…`037`"), per this section's own header note that `PNM-S-030` — not an inline edit to `iteration-1.md` — is where the annotation lives |
| ~~**PNM-G-031**~~ | ~~Order dedup key~~ | ~~first order per `sr_id`~~ | ~~per `order_id`~~ | — | **CLOSED 2026-09-04** — re-verified: `QUALIFY ROW_NUMBER() OVER (PARTITION BY o.order_id ORDER BY fpo.opp_id DESC NULLS LAST) = 1`, confirming dedup is per `order_id`. Annotated via `PNM-S-030` (see `PNM-G-030`'s closure note) |
| ~~**PNM-G-032**~~ | ~~Cancelled orders~~ | ~~excluded via `status != 4`~~ | ~~no cancelled filter — all created orders count~~ | — | **CLOSED 2026-09-04** — re-verified: `CTE_ORDERS`'s `WHERE` clause carries no status/cancelled predicate of any kind. Annotated via `PNM-S-030` |
| ~~**PNM-G-033**~~ | ~~Intra-city predicate~~ | ~~`service_type IN ('Default','Default_Short')`~~ | ~~`shifting_type = 'intra_city'`~~ | — | **CLOSED 2026-09-04** — re-verified: `AND dord.shifting_type = 'intra_city'`, no `service_type` reference anywhere in `CTE_ORDERS`. Annotated via `PNM-S-030` |
| ~~**PNM-G-034**~~ | ~~TPO denominator unit — distinct orders vs. `COUNT(DISTINCT crn)`, cardinality never stated.~~ | — | **CLOSED 2026-09-04.** Checked live across 11 months / 394,174 CRN-month combinations in the TPO base population: **max orders per CRN in a single month = 1, zero CRN-months with more than one order.** CRN and `order_id` are confirmed 1:1 in this population — `COUNT(DISTINCT crn)` and `COUNT(DISTINCT order_id)` are the same number, so `orders_base` is a genuine order count, and `PNM-M-008` needs no restating |
| ~~**PNM-G-035**~~ | ~~TPO ticket join key~~ | ~~tickets joined by `order_id`~~ | ~~joined on `crn` — `HS_TICKETS` has no `ORDER_ID` at all, so iteration-1's join could never have run~~ | — | **CLOSED 2026-09-04** — re-verified: `tpo_sql`'s tickets CTE joins `LEFT JOIN PROD_CURATED.PNM_APPLICATION.ORDERS a ON hst.crn = a.crn`, confirming the join key is `crn`. Annotated via `PNM-S-030` |
| ~~**PNM-G-036**~~ | ~~Vendor-ticket predicate~~ | ~~IN-list `('Vendor-Owner','Vendor-Supervisor')`~~ | ~~`raised_by ILIKE 'Vendor%'` — a prefix match admitting any future `Vendor-*` value~~ | — | **CLOSED 2026-09-04** — same `GROUP BY raised_by` pass over `HS_TICKETS` (`PNM-G-015`) found exactly two `Vendor-*` values: `Vendor-Owner` (619,134), `Vendor-Supervisor` (331,046). No third value exists, so the IN-list and the `ILIKE 'Vendor%'` prefix match select identical rows today. The prefix match remains the safer form going forward — annotate iteration-1's IN-list as superseded, not equivalent by design |
| ~~**PNM-G-037**~~ | ~~TPO same-month attribution~~ | ~~tickets counted only if raised in the allocation-completion month — a row-level filter~~ | ~~two independent CTEs joined at `t.month = o.month`; the tickets CTE is not restricted to the order base at all~~ | — | **CLOSED 2026-09-04 — measured, and confirmed deliberate.** Checked live, 11 months / 376,631 tickets: **127,288 (33.8%) are tied to a CRN not in that same month's completed-orders base** — of those, 106,759 (28.3% of all tickets) never appear as a completed order in **any** of the 11 months at all (still in progress, cancelled, or filtered out), and 20,529 (5.5%) belong to a different month than the ticket. **Not a rare edge case — about 1 in 3 tickets in the numerator come from an order outside that month's denominator.** Confirmed deliberate, not a defect: an independently-sourced query (headed "Source: card #47576 (TPO Trend) logic," the same card the catalog already mirrors) uses the **identical** unrestricted-join pattern — tickets and orders are two separate CTEs joined only on month, with no order-level restriction. This is official TPO methodology, not a catalog shortcut. Caveat added to `metrics.md`'s `tpo` section (`PNM-M-008`) |
| ~~**PNM-G-040**~~ | ~~p80 metric ids~~ | ~~`_mins`-suffixed (`p80_trip_duration_mins`, …) — and `reference/README.md`, tracked, still maps the baseline CSV to these names~~ | ~~the automation's exact output-column names, lowercase, no `_mins`~~ | — | **CLOSED 2026-09-04** — `reference/README.md`'s mapping table updated to the shipped ids (`p80_trip_duration`, `p80_vendor_accepted_to_sup_assigned`, `p80_sup_assigned_to_trip_started`, `p80_trip_started_to_shifting_started`, `p80_shifting_started_to_pickup_complete`, `p80_pickup_complete_to_order_complete`, `p50_trip_duration`). Also resolved in passing: the file's old "confirm which vendor accept is meant" note — the shipped SQL uses `VENDOR_OWNER_ACCEPTED_TS_IST`, settled |

## D. Live-schema and source conflicts

| id | gap | conflicting positions | next_action | status |
|---|---|---|---|---|
| ~~**PNM-G-023**~~ | ~~The Notion schema guide disagrees with the live warehouse in five places.~~ | — | **CLOSED 2026-09-04 — regenerated the actual Notion page from a live schema check, not just annotated.** Found the real page via Notion search (the KB's prior citation only pointed to a secondhand mention, not the page itself — fixed as part of this, `PNM-S-035`). All five disagreements corrected at source: `IS_MODIFICATION_DONE` BOOLEAN→TEXT, `OTA_FLAG` BOOLEAN→TEXT, `PNM_SUPPORT.MODIFICATION_CATEGORY_LIST` VARCHAR→ARRAY, `PNM_SUPPORT`'s three flag columns BOOLEAN→TEXT, `USER_FLAG` added to both `DIM_PNM_OPPORTUNITY` and `DIM_PNM_ORDERS`. Also completed the `PNM_EXPERIENCE` column list (55→71, 16 columns added, verified via a programmatic diff against live `INFORMATION_SCHEMA`, not manual comparison) and added `PNM_ALLOCATION`/`PNM_FARE_MOVEMENT` as marts (`PNM-T-016`/`T-017`), which were entirely absent. Every correction is live on the Notion page now, not just recorded here |
| ~~**PNM-G-024**~~ | ~~OTA's 30-minute clock is settled. What is NOT settled is whether a distance test applies, and what it measures.~~ | — | **CLOSED 2026-09-04 by owner ruling** (`owner-ruling:2026-09-04`). **Owner adopted a FOURTH source, not one of the three governed dbt definitions this gap had narrowed to: Metabase card #37409 ("On Time Arrival %"), ruled 30 min AND 2 km.** This is the exact "Pipeline: 30 min + 2 km" framing the 2026-08-27 restatement had declared struck-through/wrong — that restatement was correct that the three *dbt-layer* models don't fork on event, but incomplete: it didn't know a fourth, real, owner-preferred definition existed outside that layer. Card #37409 anchors on the same event as all three dbt definitions (latest `supervisor_actions` row, `action='ShiftingStarted'`), reads pickup location from `PNM_APPLICATION_SR_LOCATION_DETAILS` (not `PNM_EXPERIENCE.distance_km` — so `PNM-G-098`'s question about what `distance_km` measures is now decoupled from this gap, not resolved by it). Verified live 2026-09-04: query runs against `PROD_ELDORIA.RAW.PNM_APPLICATION_SR_LOCATION_DETAILS`/`SUPERVISOR_ACTIONS` (confirmed to exist with the exact columns used; the card's own `DEV_ELDORIA.RAW` tables are byte-identical mirrors — same row counts, same `MAX` timestamps), OTA% 86.9%–91.5% across 7 sampled months, sane. **Built into the catalog** as section `ota` (7 new metrics, headline `ota_pct`) — see `metrics.md` §7, `data-model.md`, `DECISION_LOG:D11`. Riders on `pnm_ota_capacity` (hardcoded 6-city mapping, absent-data-as-0%, `ShiftingStarted`-only denominator) **do not apply** — the adopted definition doesn't use that model |
| ~~**PNM-G-025**~~ | ~~The p80 settling period was derivable but not ratified — needed an owner call, not more analysis.~~ | — | **CLOSED 2026-09-07 by owner ruling** (`owner-ruling:2026-09-07`, `DECISION_LOG:D19`). **A `p80_durations` number is provisional while its scheduled month is within `PNM_EXPERIENCE`'s trailing 3-month rebuild window, and permanently settled once it ages out** — not because old data is deleted (full history stays in the table), but because the daily rebuild stops re-processing it. Recorded at `PNM-B-074`, upgraded `unverified` → `verified`. ⚠ The 3-month window is mart config, not fixed forever — re-check `PNM-T-101` before relying on this again after a long gap |
| ~~**PNM-G-026**~~ | ~~`p80_vendor_accepted_to_sup_assigned` runs ~2,500–2,800 minutes (~2 days) in every baseline month — an order of magnitude above every other stage.~~ | — | **CLOSED 2026-09-04 — confirmed genuine, not a data/definition bug** (`owner-ruling:2026-09-04`, session stand-in, not yet a `DECISION_LOG:D<n>`). The stage really does run ~2,500–2,800 minutes. The definition is correct; this is real operational latency between vendor-owner acceptance and supervisor assignment, not a measurement artifact |
| ~~**PNM-G-095**~~ | ~~A governed model's docs and its own config disagree on refresh cadence.~~ | — | **CLOSED 2026-09-04 — confirmed in-session** (`owner-ruling:2026-09-04`, session stand-in, not yet logged as a `DECISION_LOG:D<n>`): `pnm_ota_capacity` **is not currently refreshed at all.** The `refresh_frequency: "daily"` in its `.yml` is wrong; `tags=['exclude_daily']` was the accurate signal. `.yml` correction at source (out-of-repo) still owed, but the practical question — is this model's data currently trustworthy for freshness-sensitive use — is answered: **no.** Does not affect the shipped `ota` catalog section, which never reads this model (`PNM-G-024`) |
| ~~**PNM-G-098**~~ | ~~It is not established what `PNM_EXPERIENCE.distance_km` measures.~~ | — | **CLOSED 2026-09-04 — read directly (supplied in-session, no file/SHA citation, so evidence not a rung-3 sourced fact).** `distance_km` = `ST_DISTANCE(TO_GEOGRAPHY('POINT(...)' from pickup lon/lat), sse.event_location) / 1000`, where `sse` is the **first** `ShiftingStarted` `supervisor_actions` row per order (`ORDER BY event_ts_ist ASC, id DESC`). **It is a genuine proximity test** — pickup location vs. the `ShiftingStarted` GPS event — the same *kind* of measurement as the documented `pickup_km_deviation`, not trip distance. `PNM-T-100`'s `OTA_FLAG` **does test proximity**, resolving the original "only one of three definitions tests proximity" concern. ⚠ **Residual, minor curiosity, not blocking**: `distance_km` picks the *earliest* `ShiftingStarted` event, unlike other definitions in this family that pick the *latest* — so it and `pickup_km_deviation` may still not be byte-identical columns, just the same conceptual measurement. Not investigated further; doesn't affect the shipped `ota` section, which uses neither column (`PNM-G-024`) |
| ~~**PNM-G-099**~~ | ~~The governed `is_test_user` is not the complement of the catalog's user filter.~~ | — | **CLOSED 2026-09-04** — `GROUP BY user_flag` on all of `PROD_ELDORIA.CORE.DIM_PNM_ORDERS` returns exactly **2** distinct values, no `NULL`s: `NORMAL` (1,557,017) and `TEST` (5,792). Neither theorized divergence occurs in live data: no NULL rows exist to be dropped-vs-kept, and no prefix variant like `'Normal_Test'` exists. `user_flag ILIKE 'normal'` and `COALESCE(user_flag,'Normal') NOT ILIKE 'Normal%'` therefore select **exactly complementary** rows today. **Same pattern as `PNM-G-015`/`PNM-G-036`: agreement is a fact about current data, not a proof of equivalence** — a future third `user_flag` value (or a NULL) would silently re-diverge the two filters. A test-order count from `is_test_user` can be subtracted from a catalog count today, but re-check this row's premise before relying on that in a future session |
| **PNM-G-027** | **Should `p80_vendor_accepted_to_sup_assigned` be answerable in plain English?** It is currently hidden by having no aliases. **The original reason recorded for hiding it was false** — "its name contains 'vendor' so it hits `UNSUPPORTED_TERMS`" — but bare `vendor` is not in the guard list, the guard runs on the question not the metric name, and adding it would break `tpo_vendor_raised`. It is a legitimate stage metric published in the baseline like its visible siblings | Owner decision, leaning **~55% keep hidden**. Expose (add an alias + an ANSWERABLE case) or confirm hidden — and either way correct the recorded reason | **BLOCKED** — owner |

## E. Document-vs-document and document-vs-code

| id | gap | next_action | status |
|---|---|---|---|
| ~~**PNM-G-050**~~ | ~~Metric count: `HANDOFF.md` §3 and `iteration-2` §1 both say the registry holds 25 metrics.~~ | — | **CLOSED 2026-09-04** — confirmed live (`len(metrics_registry.METRICS)` = 54, 7 sections), then corrected both files at source: `HANDOFF.md` line 44 and `iteration-2-readiness-ledger.md` line 14 now read "54" with a `[2026-09-04, PNM-G-050]` marker, sections listed out to match the current 7 (was 4) |
| ~~**PNM-G-052**~~ | ~~No mapping exists between the registry metric ids and the 167 Argus DD ids~~ | — | **CLOSED 2026-08-26** → [metrics.md](./metrics.md) §19 (renumbered twice since: §9 originally, then §10 when `gac_ctr` was inserted as §8, now §19 after the 10 more MBR sections were inserted as §9-§18, all 2026-09-04). **42 of 47** catalog metrics map to **41** Argus rows; 5 are unmapped by design (4 raw channel order counts the DD does not track, plus the `orders_base` denominator). **16 Argus rows move `pending` → `partial`** once p80/order_edits are reflected. The mapping also independently confirmed three things — see §19.3. ⚠ **The mapping itself is now stale** — it predates the 82 metrics added in `DECISION_LOG:D13` and was never extended to them (explicitly out of scope for D13) |
| ~~**PNM-G-053**~~ | ~~`docs/overview.html` is pre-iteration-3 and wrong in three ways.~~ | — | **CLOSED 2026-09-04 — fixed at source, all three.** (1) Readiness table: `p80_durations`/`order_edits` moved from "not built" to "prototype-only" with their real validation evidence (`V4`), and `ota` moved from "blocked" to "prototype-only" (`D11`/`V5`) since it wasn't even accurate as "blocked" anymore. (2) Test count: confirmed live (`run_tests.py` → 62 passed, 0 failed) and updated the iteration-3 card + footer to 62/62, dated. (3) The "Open decision: which orders source?" section rewritten as **Resolved — Option A** with `DECISION_LOG:D3`'s outcome, rather than presenting a settled question as live. Iteration 3's badge also flipped `Next` → `Done`, and the footer's built-sections list extended from 4 to 7 — both directly entailed by fixing (1), left inconsistent otherwise |
| ~~**PNM-G-054**~~ | ~~The committed dry-run report is not reproducible.~~ | — | **CLOSED 2026-09-04 — fixed at source (parameterized, not just frozen).** `run_tests.py`'s `main()` now takes an optional `today: date` (defaults to the real date for normal use); both `is_month_in_progress()` calls and the `is_month_in_future()` call now receive it explicitly instead of each independently calling `date.today()`. A `--today=YYYY-MM-DD` CLI flag pins it. **Verified: two runs with the same `--today` produce a byte-identical `dry_run_report.md`.** Regenerated the committed report at `--today=2026-09-04`; suite still 62/0 |
| ~~**PNM-G-055**~~ | ~~Five pairs of commits carry identical messages on divergent branches.~~ | — | **CLOSED 2026-09-04 — checked, doesn't touch PnM.** `git log --all` confirms all 5 duplicate-message pairs are **PTL** knowledge-base commits (`WALKTHROUGH.md`, "M-number conflation", etc.) landing on two branches — nothing PnM-related. **No PnM `repo@<sha>` citation in this KB is at risk.** The standing practice (always copy the SHA, never re-derive from a message) remains correct going forward, same disposition as `PNM-G-064` |

## F. Naming and jargon

| id | gap | next_action | status |
|---|---|---|---|
| ~~**PNM-G-060**~~ | ~~"Packers & Movers" — the expansion of PnM — appears in no PnM or PTL source file in this repo.~~ | — | **CLOSED 2026-09-04** — confirmed in-session (`owner-ruling:2026-09-04`): PnM = **Packers & Movers**. `PNM-B-050` updated from `unverified`/`local:` to **`verified`**, source now the in-session ruling rather than the out-of-repo workspace file |
| ~~**PNM-G-061**~~ | ~~`CRN` is never expanded in any source.~~ | — | **CLOSED 2026-09-04** — confirmed in-session (`owner-ruling:2026-09-04`): CRN = **Customer Reference Number**. `PNM-B-056` updated to **verified**. The sub-question raised the same day (does `crn LIKE '%PNM%'` diverge from a `LBR%`-exclusion rule used elsewhere) was already checked live and found no divergence on `FACT_PNM_ORDERS` — closed alongside |
| ~~**PNM-G-062**~~ | ~~`LMS` is never expanded.~~ | — | **CLOSED 2026-09-04** — confirmed in-session (`owner-ruling:2026-09-04`): LMS = **Lead Management System**, consistent with its existing placement inside the `Others`/`Generic` channel bucket. `PNM-B-062` updated to **verified**. Unblocks confirming whether `PNM-065` ↔ `pct_orders_others` ([metrics.md](./metrics.md) §19.3) is a safe mapping — not separately re-checked here |
| ~~**PNM-G-063**~~ | ~~`OTA`, `P80` and `TPO` expansions trace only to the coverage map or to code, never to a governed definition.~~ | — | **CLOSED 2026-09-04 — the premise was already false when re-checked.** `business.md` already carries a glossary row for all three, each with a `source_ref`: **`PNM-B-052`** TPO = Tickets Per Order (`metrics_registry.py`, verified), **`PNM-B-058`** P80 = 80th percentile (`pnm-gem-knowledge.md` §2.5, unverified), **`PNM-B-059`** OTA = On-Time Arrival (`owner-ruling:2026-09-04`, verified — the strongest of the three). All three are folded into a tracked KB source and cited, which is exactly what this row's `next_action` asked for. ⚠ **Not the same claim as "governed at rung 1-3 for all three"** — `PNM-B-052` and `PNM-B-058` still cite code/prose rather than an owner ruling, correctly reflected in their existing `verified`/`unverified` grades. That confidence nuance is not this row's concern; it already asks the right question of each source |
| ~~**PNM-G-064**~~ | ~~Cross-vertical metric-name collisions.~~ | — | **CLOSED 2026-09-04 by ruling (`owner-ruling:2026-09-04`) — the mandatory `PNM-` prefix (CONTRIBUTING §3) is confirmed as the mitigation, nothing further to decide.** Not a fill-in-the-blank like the other rows in this group — the standing rule already existed; this closes it as accepted rather than resolving new information. A cross-vertical disambiguation layer remains an Argus-level concern, out of scope for this KB |

## G. Coverage — what this KB does not cover

| id | gap | next_action | status |
|---|---|---|---|
| **PNM-G-070** | **The catalog cannot be cut by city or by week, and that is exactly what city ops will ask for.** The columns exist, the dashboards do it, but **no city- or week-level query has ever been reconciled**, so the catalog refuses. **This is the largest gap between this KB and its audience** | Owner/analytics decision: validate a city and weekly variant of the six sections, or route city ops to `PNM-S-020`/`PNM-S-021` permanently and say so out loud. ⚠ **The ground shifted 2026-08-26 on two fronts:** a **governed `pickup_city_name` dimension now exists** on the leads semantic model (`PNM-S-051`), so for **leads** a city cut is a semantic-layer query rather than new work; and **`pnm_ota_capacity` already publishes `ota_percentage` by city and slot** (`PNM-S-054`), so a city cut exists in production for at least one measure. The decision is now materially cheaper than when this gap was opened. ⚠ **But cheaper is not free — priced properly 2026-08-27.** `pnm_ota_capacity`'s city cut is a **hardcoded `CASE` over `geo_region_id IN (1,2,3,4,5,8)`** covering six cities, written out twice in the file, with no link to `dim_geo_regions` — which `dim_pnm_opportunity` *does* FK-test against (`PNM-T-108`). Its slot buckets put **00:00-03:59 into "evening"**, and its "outstation" load is **intra-city by construction** and dumped entirely into the morning slot (`PNM-T-112`). Adopting it delivers a city cut for **six cities on a private mapping**, not a governed one | **BLOCKED** — owner *(materially changed 2026-08-26; cost re-priced 2026-08-27)* |
| ~~**PNM-G-071**~~ | ~~The MBR automation runs 14 sections; this catalog covers 6.~~ | — | **CLOSED 2026-09-04.** Narrowed once (real count was 11 uncovered groups, not 8), then closed the same day: all 10 remaining groups were built (`DECISION_LOG:D13`, `V7`) after `gac_ctr` (`D12`) proved the pattern. **Catalog now covers all 15 metric groups / 137 metrics the MBR automation runs** — `fare` (14), `vendor_earnings_pctl` (6), `allocation` (33, the largest group), `wallet` (6), `vendor_tpo_top5` (6), `addon` (7), `completion` (3), `weekend` (1), `cac_post_trip` (1), `vendor_earnings_bucket` (5), plus `gac_ctr` (1) the day before. Every new section live-reconciled for 2026-05, two with real independent cross-checks (`vendor_earnings_pctl`'s order count vs TPO's `orders_base`; `vendor_tpo_top5`'s `vendor_tpo` vs this catalog's own `tpo_vendor_raised`). **Not done**: cross-mapping the 82 new metrics against the 167-row Argus DD (a separate, sizeable task, explicitly deferred by `D13`) |
| ~~**PNM-G-072**~~ | ~~Two governed marts the catalog does not read — `PNM_ALLOCATION` and `PNM_FARE_MOVEMENT`.~~ | — | **CLOSED 2026-09-04 — evaluated, confirmed correct.** `PNM-S-060` (the MBR automation's real SQL) confirms both marts are exactly right for their purpose: `PNM_ALLOCATION` sources all 33 allocation-quality metrics, `PNM_FARE_MOVEMENT` sources fare/coupon/surge (14) and vendor P50/P80 earnings (6). **Evaluation is done — building the code to actually read them is now `PNM-G-071`'s scope**, not a separate open question. The `PNM-G-073` half of this row's next_action was already closed separately (immaterial, no fix needed) |
| ~~**PNM-G-073**~~ | ~~Test orders are largely NOT excluded, and nothing says so to a reader.~~ | — | **CLOSED 2026-09-04** — sized live off `PROD_ELDORIA.MART.PNM_ALLOCATION.IS_TEST_USER`, grouped by month, 10 months (2025-11 to 2026-08): test-order share ranged **0.024%–0.590%** of monthly order volume (e.g. 2026-05: 72 of 61,715 = 0.117%), never above 0.6% in any month. **Immaterial — no filter change made.** Caveat added directly to `p80_durations` (`PNM-M-020`) and `order_edits` (`PNM-M-030`) in `metrics.md` instead. ⚠ **This is a proxy, not a same-table measurement**: `PNM_ALLOCATION` and `PNM_EXPERIENCE` (the actual source for `p80_durations`/`order_edits`) are different tables — `PNM_EXPERIENCE` carries no `user_flag`/`is_test_user` column at all, so its true test-order share was not measured directly, only inferred from the shared `dim_pnm_orders.user_flag` lineage. leads/orders were already covered by their existing `user_flag ILIKE 'normal'` filter, now confirmed equivalent to `is_test_user` on live data (`PNM-G-099`, closed alongside this row) |
| **PNM-G-074** | **87 PnM dashboards exist; 17 are registered here** and filters were verified card-by-card for only 4 of them. Several sit in personal collections and may 403 | Resolve filters for the dashboards ops actually uses; ask in `#pnm-analytics` for access to the personal-collection ones | **OPEN** |
| ~~**PNM-G-075**~~ | ~~The Gem KB's §5 SQL template library is not reproduced in this KB.~~ | — | **CLOSED 2026-09-07 by owner ruling** (`owner-ruling:2026-09-07`). **This KB carries formulas, never runnable SQL.** `tests_output/rendered_*.sql` (`PNM-S-004`, regenerated fresh by every `run_tests.py` run) is the one executable record — not duplicated into `metrics.md`, so there is exactly one copy of any query's actual SQL and it cannot drift from what the prototype runs. `pnm-gem-knowledge.md` §5's six templates stay as historical narrative, superseded, not migrated |
| ~~**PNM-G-076**~~ | ~~140 of the 167 Argus DD metrics are `pending` / not started, and 2 are `blocked` on an unavailable source.~~ | — | **CLOSED 2026-09-04 — figure was stale, corrected.** Live count from `coverage-map/metric-coverage.json`: **124 `pending`, 43 `partial`, 0 `blocked`** (167 total) — the `blocked` bucket has emptied out entirely and 16+ more rows moved to `partial` since this row was written, consistent with the mapping work behind `PNM-G-052`. Not this KB's work to close (per its own original framing), but the honest current denominator for any "PnM metric coverage" claim is now **124/167 pending**, not 140/167 |
| **PNM-G-080** | **Five live data-integrity issues affect neighbouring dashboards** (`PNM-S-040`…`S-044`) — the stalled `ameyo_webhook_events` feed since June 2026 being the most consequential still-open one | Track to closure in `#pnm-analytics`; re-check before quoting any dashboard that depends on them | **OPEN** |
| ~~**PNM-G-100**~~ | ~~`gac_ctr_pct` and Argus DD row `PNM-031` share the identical name "Get a Call CTR" but may not be the same population — `PNM-031`'s `system` field says Amplitude, the built metric is pure Snowflake.~~ | — | **CLOSED 2026-09-04 — confirmed in-session** (`owner-ruling:2026-09-04`): the metric is **Snowflake-only, as originally supplied** — the automation's `gac_ctr_metrics` CTE is the intended definition, not a divergent Amplitude one. `PNM-031`'s `system: Amplitude` tag in the coverage map is stale/not the operative source. Mapped in `metrics.md` §19.1; `PNM-031`'s coverage-map status moved `pending` → `partial`, same treatment as `PNM-106`/`107` when `ota` was built |

## H. The governed dbt layer vs this catalog

*Opened 2026-08-26, when `porterin/DE-DBT-SNOWFLAKE` entered the KB's sources as rung 3.*

| id | gap | conflicting positions | next_action | status |
|---|---|---|---|---|
| ~~**PNM-G-090**~~ | ~~The governed `pnm_overall_leads` and this catalog's `leads_overall` do not compute the same number~~ | — | **CLOSED 2026-08-26 by owner ruling** (`owner-ruling:2026-08-26`). **Both are correct and they measure different things.** `pnm_overall_leads` is PnM lead volume **across all shifting types**; this catalog's metric is the **intra-city** subset and is to be renamed **`leads_overall_intra_city`** to say so. Neither is retired. → the rename itself is `PNM-G-093` |
| ~~**PNM-G-093**~~ | ~~The `leads_overall` → `leads_overall_intra_city` rename is ruled but NOT implemented.~~ | — | **CLOSED 2026-09-04.** Renamed across all call sites in one change: `metrics_registry.py` (`METRICS` key, `conversion_overall`'s `denominator`), `sqlgen.py` (`AGG_LEADS` alias, `funnel_sql` select list), `run_tests.py` (2 `ANSWERABLE` cases). Definition text now states **intra-city-only** explicitly and cross-references `pnm_overall_leads` so the two "PnM leads" figures are never conflated. `run_tests.py --today=2026-09-04` → **110/110 pass**. Re-reconciled 2026-05 live: **336,291** vs the `V3` anchor of 336,338 — a 47-lead (0.014%) drift from late-arriving/backfilled records, not the rename (identical SQL, alias-only diff, confirmed by rendering the pre- and post-rename query side by side). Evidence: `DECISION_LOG:V8` |
| **PNM-G-091** | **~20 PnM semantic models and ~25 doc ymls in the governed repo are unmined** — `dim_pnm_orders`, `fact_pnm_orders`, `fact_pnm_opportunity`, `pnm_customers`, `pnm_support`, `pnm_allocation`, `pnm_fare_movement`, **`pnm_ota_capacity`**, `pnm_base_query`, `pnm_growth_card`, `mart_pnm_invoices`, and the vendor/fare/rechurn models. Three files mined so far settled two owner-blocked gaps and opened one conflict — **the unmined remainder is likely to do the same** | — | ✅ **Five more mined 2026-08-27** — `pnm_ota_capacity`, `pnm_support`, `pnm_allocation`, `pnm_fare_movement`, `fact_pnm_orders`/`dim_pnm_orders`. They disproved `PNM-G-024`'s event fork, supplied `PNM-G-073`'s sizing method, priced `PNM-G-070`, and opened six rows (`PNM-G-094`…`099`). **A ranked discovery loop now exists** (`PNM-S-058`) so the remainder need not be picked by guesswork. ⚠ **Two orderings, and they are opposite:** by *documentation debt* (mine the worst first) or by *evidentiary yield* (mine the best first, because `CONTRIBUTING §5` only lets a merged **test** clear `verified`, so a gates-failing model is prose you must grade `unverified` regardless). The gap-first order used on 2026-08-27 deliberately took the debt ordering, and three of the five targets were gates-failing `C` grades. **Say which ordering you are using.** ✅ **Four more mined 2026-09-07, evidentiary-yield ordering: `fact_pnm_opportunity` (97.5 A), `dim_pnm_vendor` (95 A), `pnm_gst_daily` (100 A), `cge_pnm_paid_lead_attribution` (100 A).** Findings: (1) `dim_pnm_vendor`'s true grain is `(vendor_id, bucket_start_date)`, not `1 vendor` — `data-model.md` `PNM-T-124` corrected; this catalog's own `wallet_sql` only ever does `SELECT DISTINCT vendor_id` against it, so the composite grain never fanned anything out — **checked, not a defect** (`PNM-T-131`). (2) `fact_pnm_opportunity`'s doc confirms `opp_id` grain and non-unique `opp_uuid`, both already assumed correctly; it also carries an LSM-score pair (`opp_latest_score`/`hash_score`) parallel to but distinct from the table `gac_ctr` actually reads (`PNM-T-119`) — flagged so a future LSM question doesn't assume one source (`PNM-T-132`). (3) `pnm_gst_daily` — mined, zero table overlap with this catalog (GST/finance mart, `CP_FINANCE_PRODUCT`), nothing further to extract. (4) `cge_pnm_paid_lead_attribution` — mined, zero table overlap, but it's **a third thing also called "PnM leads"** (marketing-attribution leads, not opportunity-funnel leads) — recorded at `metrics.md` §1 next to the existing two-leads note so a future question doesn't conflate three concepts that share a name. **Net effect of this round: zero new gaps opened, one doc correction, two documented non-conflicts, one ownership/naming clarification.** Next by evidentiary yield: `pnm_gst_daily`'s sibling marts and the remaining vendor/fare/rechurn models — untried, no scores fetched yet this round. ⚠ **Ruled 2026-09-07: this gap is NOT closable, by design — same shape as `PNM-G-004`.** The governed repo keeps growing PnM models; "all models mined" is a moving target, not a fact this KB can ever mark true and move on from. Each mining round narrows it (settles conflicts, corrects doc facts, rules out risks) but there is no finish line to reach — treat it as a standing, recurring task to pick up periodically, not a backlog item awaiting a final round. ⚠ **Note the contrast with `PNM-G-007`** (closed the same day) — that gap's root cause was a *fact about the world* that could become false (the mart finishing construction) and did; this gap's root cause is that the *repo itself keeps growing*, which has no comparable end state | **OPEN** |
| **PNM-G-094** | **Two governed models publish absent data as a bad outcome.** | `pnm_ota_capacity` ends `MAX(CASE WHEN slot = … THEN ota_percentage ELSE 0 END)`, so a city/date/slot with no OTA row publishes **0%** — indistinguishable from a real 0%. `pnm_support.on_time_arrival_flag` uses `ELSE 'No'`, labelling an order with no `ShiftingStarted` action **delayed** (`PNM-T-109`). Contrary to the divide-by-zero-returns-null rule this workspace treats as non-negotiable | Raise with `NI_PNM` — both want `NULL`, not `0`/`'No'`. Until fixed, **never quote an OTA % of 0 from `pnm_ota_capacity`** without checking the slot had orders | **OPEN** |
| ~~**PNM-G-096**~~ | ~~`pnm_ota_capacity`, `pnm_support` and this catalog's own `ota` section all pick a "latest action" with `RANK()`, not `ROW_NUMBER()` — an unmeasured tie risk.~~ | — | **CLOSED 2026-09-04 — measured live**, `PROD_ELDORIA.RAW.PNM_APPLICATION_SUPERVISOR_ACTIONS`, `action = 'ShiftingStarted'`: **880 of 2,107,392 orders (0.042%) have an exact `event_ts_ist` tie at the max** (always exactly 2 rows, never more) — the risk is real, not hypothetical. **Zero of those 880 fall inside the 2026-05 reconciled `ota` population** (49,400 orders), so `V3` and this section's own reconciliation are unaffected. **Not fixed** — swapping to `ROW_NUMBER()` + a secondary tiebreak would be an unreviewed definition change to code that mirrors an owner-ruled source (Card #37409); logged and bounded instead, recorded at `metrics.md` §7 (`ota`). If a future month shows drift, re-run this measurement for that month before suspecting anything else. Evidence: `DECISION_LOG:V9` |
| **PNM-G-097** | **The governed repo's validation artifacts cannot be counted naively, which affects any trust-based discovery loop.** | Of 40 PnM `*_validation.json` files, **11 are byte-identical duplicates** across `models/docs/X.json` and `models/docs/mart/X.json` (`cmp` confirms), so 40 files describe **29** models. And **`mart_pnm_invoices_validation.json` uses a different schema entirely** — `trust_score` as a bare int, `trust_grade` as a sibling key, no `scoring_version`, no `validated_at` — so its `100/A` is **not comparable** to the v5 scores and sorts to the top spuriously | Dedupe by `model` and handle both schema shapes before ranking. Worth raising the duplication with the repo owners; it is theirs, not PnM's | **OPEN** |
| ~~**PNM-G-092**~~ | ~~The tables this catalog reads have at least three different owners, and the KB had recorded none of them.~~ | — | **CLOSED 2026-09-04 by owner ruling** (`owner-ruling:2026-09-04`, `PNM-T-130`). **`dim_pnm_opportunity` and `pnm_experience` → PnM Analytics team; the `PROD_CURATED.PNM_APPLICATION.*` raw tables → PnM Engineering team.** This is the business-accountable owner for PnM's purposes — it does not overwrite or resolve the governed repo's own `owner:` tags (`PNM-T-103`, still HSC / DATA_ANALYTICS), and does not settle the `dim_pnm_opportunity` HSC-vs-`NI_PNM` self-contradiction in that repo, which remains a question for the model's author |

---

## Summary

| Group | Rows | Of which `BLOCKED` on an owner decision |
|---|---|---|
| A. Provenance & citation integrity | 6 | 2 |
| B. Code behaviour no document ratifies | 15 | 1 |
| C. iteration-1 vs owner rulings | 9 | 0 |
| D. Live-schema and source conflicts | 8 | 1 |
| E. Document-vs-document / vs-code | 5 | 0 |
| F. Naming and jargon | 5 | 0 |
| G. Coverage | 9 | 1 |
| H. Governed dbt layer vs this catalog | 7 | 0 |
| **Total rows** | **64** | **5** |

*64 rows exist; **52 are CLOSED** (`PNM-G-002`, `PNM-G-007`, `PNM-G-008`, `PNM-G-011`, `PNM-G-012`,
`PNM-G-014`, `PNM-G-015`, `PNM-G-016`, `PNM-G-017`, `PNM-G-018`, `PNM-G-019`, `PNM-G-020`,
`PNM-G-021`, `PNM-G-023`, `PNM-G-024`, `PNM-G-025`, `PNM-G-026`, `PNM-G-030`, `PNM-G-031`,
`PNM-G-032`, `PNM-G-033`, `PNM-G-034`, `PNM-G-035`, `PNM-G-036`, `PNM-G-037`, `PNM-G-040`,
`PNM-G-041`, `PNM-G-042`, `PNM-G-043`, `PNM-G-050`, `PNM-G-052`, `PNM-G-053`, `PNM-G-054`,
`PNM-G-055`, `PNM-G-060`, `PNM-G-061`, `PNM-G-062`, `PNM-G-063`, `PNM-G-064`, `PNM-G-071`,
`PNM-G-072`, `PNM-G-073`, `PNM-G-075`, `PNM-G-076`, `PNM-G-090`, `PNM-G-092`, `PNM-G-093`,
`PNM-G-095`, `PNM-G-096`, `PNM-G-098`, `PNM-G-099`, `PNM-G-100`), leaving **12 live** — 7 `OPEN`,
5 `BLOCKED`.*
*The group column above sums to 64, and has always summed to whatever the rows actually are. Until 2026-08-26 this file published **four** wrong
figures from one root cause — a `Total rows` of 58 that never matched its own groups, and the derived
"58 rows exist / 56 live / 48 `OPEN`" prose (correct at the time: 57 / 55 / 47). The **10 CLOSED**
figure that stood here through 2026-09-04 was the same failure mode recurring: it undercounted by 5,
missing every `PNM-G-030`…`035` closure (the iteration-1-vs-rulings rows closed 2026-09-04, group C).
**Recount from the group column on every edit; never carry a total forward.***

**The five owner-blocked rows are the ones that cannot be closed by more analysis:** `PNM-G-003`
and `PNM-G-006` (put the automation under version control), `PNM-G-022` (correct the stale
"canonical" comment at source), `PNM-G-027` (expose the vendor stage metric or not), and `PNM-G-070`
(city/weekly cuts — now **materially cheaper**, since a governed `pickup_city_name` dimension
exists). ✅ **`PNM-G-041` and `PNM-G-025` ratified and closed 2026-09-07** — no longer blocked, see
`DECISION_LOG:D18`/`D19`.

✅ **`PNM-G-024` closed 2026-09-04 by owner ruling** — resolved by adopting a fourth source
(Metabase card #37409) outside the three governed dbt definitions this gap had narrowed to, not by
choosing among them. `ota` is now built — 7 new metrics, `metrics.md` §7, `DECISION_LOG:D11`. Two
downstream rows moved with it: `PNM-G-043` (superseded — the six-column blocker no longer applies)
and `PNM-G-021` (closed — `base_population` corrected as part of the build).

✅ **`PNM-G-002` closed 2026-08-26 — by checking, not by doing.** Both files it called untracked were
already tracked, the spec since **38 days before** this KB was built. Three citations moved `local:` →
`repo@<sha>:`, and `PNM-G-063`'s stated precondition is now met. The grade did **not** move — `§4.1`'s
ceiling and `§5`'s document rule are separate caps, and only the first one lifted. ⚠ **Its
`next_action` was actively wrong**: it told the reader to copy the spec in from the Desktop clone,
where the surviving copy is an *older* pre-board-review draft. **A stale `next_action` is a hazard,
not just noise** — re-check a provenance row's premise before executing it.

✅ **`PNM-G-090` closed 2026-08-26 by owner ruling** — the two lead metrics measure different
populations and both stand; ours is renamed to say so. The rename shipped 2026-09-04 (`PNM-G-093`,
closed) — the id is now `leads_overall_intra_city` everywhere in code and docs.

✅ **`PNM-G-024` and `PNM-G-025` both narrowed on 2026-08-26, then both closed** — the governed dbt
layer supplied the facts each was waiting for, and the owner subsequently adopted what the evidence
showed: `PNM-G-024` via `DECISION_LOG:D11` (2026-09-04), `PNM-G-025` via `DECISION_LOG:D19`
(2026-09-07).

✅ **`PNM-G-008` closed 2026-09-04** — all three cards fingerprinted via the Data Catalog (`PNM-S-010`
2026-03-30, `PNM-S-011` 2026-05-08, `PNM-S-059` 2025-12-08). `PNM-G-007` was
re-verified 2026-09-04 (no schema drift since 2026-07-29) and stayed open at the time — a schema
pre-flight is inherently a recurring check while its root cause (the mart's "active construction"
status) remains true. ✅ **That root cause itself changed 2026-09-07** — the owner declared
`PNM_EXPERIENCE` in final shape and form, so `PNM-G-007` closed outright (`DECISION_LOG:D20`); this
is a genuine exception to "schema-pre-flight gaps don't close," not a contradiction of it. `PNM-G-004`
was **narrowed, not closed** — self-consistency evidence was added for 2026-06 (channel splits sum
correctly; the 5 TPO stage buckets partition `tpo_overall` exactly), but true reconciliation against
the automation stays blocked on the same root cause as `PNM-G-003`/`PNM-G-006` (no repo access to the
automation).

✅ **`PNM-G-073` and `PNM-G-099` closed 2026-09-04 by live sizing, no owner input needed.** Test
orders run **0.024%–0.590%** of monthly volume (10 months checked) — immaterial, so no filter was
added; a caveat was written directly into `p80_durations`/`order_edits` in `metrics.md` instead. And
`DIM_PNM_ORDERS.USER_FLAG` carries only two live values (`NORMAL`/`TEST`, no `NULL`s), so the
catalog's filter and the governed `is_test_user` flag — which diverge *on paper* — select identical
rows *today*. Same shape as `PNM-G-015`/`PNM-G-036`: **a measured absence of divergence, not a proof
it can never occur.**

✅ **`PNM-G-071` closed 2026-09-04 — the catalog now covers every MBR automation group.** The owner
supplied the automation's full SQL in-session (now tracked as `PNM-S-060`); `gac_ctr` was built first
to prove the pattern (`D12`), then all 10 remaining groups in one pass (`D13`) — 82 new metrics, 137
total, 18 sections. Two closed alongside it: `PNM-G-072` (evaluated `PNM_ALLOCATION`/`PNM_FARE_MOVEMENT`,
confirmed as the right sources, now actually read) and `PNM-G-100` (the new `gac_ctr_pct` and Argus
row `PNM-031` are the same population, owner-confirmed, not the Amplitude/Snowflake mismatch first
suspected). **Not done:** mapping the 82 new metrics against the 167-row Argus DD — out of scope for
this pass, `metrics.md` §19 is stale relative to the current catalog until that happens.
