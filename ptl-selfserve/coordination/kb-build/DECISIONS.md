# DECISIONS — PTL self-serve KB build (append-only)

*Orchestrator-only. One entry per decision, IDs strictly increasing. Scoped to the **KB build**
workstream only. The sibling `../DECISIONS.md` (D-001…D-005) is a SEPARATE, COMPLETE workstream
("corrections + Track B") and is not modified by this one. Authoritative **domain** rulings
(metric definitions, source path, North Star) live in the repo `ptl-selfserve/DECISION_LOG.md`
as D1–D7.*

[D-001] 2026-07-29 (orchestrator) — Mode = HYBRID: PARALLEL fan-out for Phases 1–3, SEQUENTIAL for Phase 4. — Step-0 test: Phases 1 (research), 2 (source inventory) and 3 (dashboard SQL extraction) each pass all four bars — decomposable without shared live output, breadth-shaped, workers write nothing shared (they return findings; orchestrator is sole writer), and worth the token cost because raw web pages / source docs / card SQL are bulky and must stay out of main context. Phase 4 (write the KB) is depth + single-writer synthesis → fails bars 1 and 2 → SEQUENTIAL.

[D-002] 2026-07-29 (orchestrator) — New coordination namespace `coordination/kb-build/`; existing `coordination/BOARD.md` + `DECISIONS.md` left byte-for-byte untouched. — The existing record belongs to a workstream marked COMPLETE. The skill forbids silently appending to a pre-existing `coordination/`; the task constraint forbids modifying existing files, which also rules out the skill's default archive-and-restart. A new sub-namespace satisfies both: only NEW files are written, and the prior trail stays intact as history. Owner-confirmed.

[D-003] 2026-07-29 (orchestrator) — `kb/` is written to the Desktop clone at `selfserve_data/ptl-selfserve/kb/` on branch `claude/ptl-metric-catalog-map`. — Owner chose "in the git repo, next to the assets". That branch is the only place the PTL assets exist (catalog, DECISION_LOG, selfserve_nlq), so it is the only location where relative links from the KB resolve and where the KB is version-controlled alongside what it indexes.

[D-004] 2026-07-29 (orchestrator) — For domain facts, `ptl-selfserve/DECISION_LOG.md` D1–D7 is the authority — NOT `coordination/DECISIONS.md`. — The task brief said "DECISIONS.md wins". But `coordination/DECISIONS.md` explicitly delegates in its own header: "Domain rulings (metric definitions, source path, North Star, etc.) live in the repo `ptl-selfserve/DECISION_LOG.md` as D1–D7; these `[D-NNN]` entries are the orchestration-level trail." Applying the brief literally would elevate an orchestration log over the domain log it defers to. Owner notified; proceeding on the delegated reading.

[D-005] 2026-07-29 (orchestrator) — Provenance in the KB is `clone + branch + path (+ line)`, not `path` alone. — Discovered two clones of `akshayjain00/selfserve_data` on diverged branches: `~/dev/selfserve` @ `claude/pnm-p80-orderedits` (full PnM: eldoria re-point, p80/order_edits, gem-knowledge/instructions, reconciliation) and `~/Desktop/AI_V2/ProdOps/selfserve/pnm/selfserve_data` @ `claude/ptl-metric-catalog-map` (full PTL). Neither branch contains both. A bare path is therefore ambiguous — the same path holds different bytes per clone — and a future session could "re-verify" a fact against the wrong file. Phase 2 reads BOTH clones; every KB fact records which.

[D-006] 2026-07-29 (orchestrator) — Model routing: Sonnet 5 for research + mechanical extraction workers; Opus for synthesis and KB authoring. — Owner instruction ("use right model for right task"). Research and SQL-extraction are recall/transcription-shaped and well within Sonnet's envelope; the cross-source conflict arbitration and KB authoring in Phase 4 are the judgment-heavy steps that justify Opus.

[D-007] 2026-07-29 (orchestrator) — Maker-checker gate run on the ORCHESTRATION PLAN before execution (Opus checker, non-blind by design). 15 findings; 12 accepted, 1 modified, 2 already-resolved. — Gating the plan costs one re-plan; gating after Phases 1–3 would also waste every worker's output. Non-blind was correct here because the artifact under review IS the reasoning; the BLIND variant is reserved for the finished KB (spec-validated against DECISION_LOG + card SQL), where my rationale would bias the checker toward the exact errors it exists to catch. Rejected-as-written: LF-4 (see D-013). Already-resolved: LF-1 (write zone — owner ratified `coordination/kb-build/` via the checkpoint question, superseding the brief's "only inside kb/" zone rule; recorded, not re-asked) and BS-6 (Notion page ID supplied by owner mid-turn).

[D-008] 2026-07-29 (orchestrator) — Provenance format changed to `repo@<commit-sha>:<path>#L<n>`. SUPERSEDES D-005. — Checker LF-2. D-005's `clone + branch + path + line` is built from coordinates that expire: branches get merged/rebased/deleted, line numbers shift on the next commit, and local clone paths move (a known transient OS lock affects clones under `~/Desktop/AI_V2`). It would have died to the very two-clone problem it was designed to survive. It also contradicted D-003, whose justification was that *relative* links resolve — one KB cannot cite both relatively and clone-absolutely. Resolution: commit-SHA permalinks are the canonical citation (immutable by construction — a SHA cannot drift, so re-verification always reads the same bytes); relative links are used for in-repo navigation; the local clone paths appear ONCE as a convenience alias block in CONTEXT.md, never repeated per fact.

[D-009] 2026-07-29 (orchestrator) — Explicit precedence ladder, published in CONTEXT.md and in every worker prompt: observed card SQL > DECISION_LOG D1–D7 > iteration-1 catalog > journey/proposal docs > Notion review. EXCEPTION: a SQL-vs-D-ruling contradiction is NEVER auto-resolved — emit both sides, mark `[unverified]`, open a GAPS row. — Checker LF-3. D-004 named DECISION_LOG the domain authority while Phase 3 mandates grounding in SQL; with the `state` enum UNCONFIRMED and cancellation on "4793 logic", collision is likely, and two authorities with no tie-break force exactly the silent pick the brief forbids.

[D-010] 2026-07-29 (orchestrator) — Add a separate ACCEPTANCE TEST agent (fresh, zero-context, Sonnet) whose entire prompt is the absolute path to `kb/CONTEXT.md` plus the two DONE-WHEN questions; grade its answers against DECISION_LOG. — Checker BS-2. The planned blind checker audits accuracy; the DONE-WHEN criterion is loadability ("a fresh session pointed ONLY at kb/CONTEXT.md can correctly answer…"). A KB can be fully accurate yet fail that test through a bad index, missing load instructions, or facts unreachable from the entry point. Without this step the stated acceptance criterion is never actually exercised.

[D-011] 2026-07-29 (orchestrator) — KB facts are addressable rows with stable IDs (`M-###` metric, `T-###` table, `G-###` gap, `B-###` business/glossary) and fixed columns: `id | statement | source | last_verified | source_updated_at | confidence`. — Checker BS-4 + BS-5. (a) Patchability: the brief requires incremental enhancement in accuracy AND coverage; prose modularity does not give that. Stable IDs make "correct a fact" a single-row edit and "add coverage" an append — diffable and mergeable, which matters because the two diverged branches already prove concurrent edits happen here. (b) Staleness: `last_verified` alone records when I looked, not whether the source moved. Carrying the Metabase card's `updated_at` turns "is this stale?" from unknowable into a one-call comparison — `source_updated_at > last_verified` ⇒ row is STALE until re-extracted.

[D-012] 2026-07-29 (orchestrator) — Phase 3 mechanics: (a) orchestrator performs Metabase auth and one probe (`get_dashboard 4198`) ITSELF before any fan-out; red probe halts Phase 3 to the owner rather than fanning out. (b) orchestrator enumerates dashboard cards first, then shards by card batches (~8/worker), not by surface. (c) every worker prompt hard-prohibits `execute_card` / `execute_query` / `sql-exec` — metadata reads only; unresolvable from SQL text means `[unverified]`, never "run it". (d) workers write fixed-schema extracts to `coordination/kb-build/extracts/`; orchestrator authors one topic file per pass, reading only that file's extract. — Checker RC-1/2/3/4. (a) OAuth is interactive and subagents cannot complete it, so an unprobed fan-out returns three auth errors at full spawn cost. (b) sharding by surface puts one card (33519) against a whole dashboard (4198) — a ~1:30 imbalance where the heavy worker truncates and silently drops metrics. (c) a worker told to "ground metrics in SQL" will reach for execution; the standing rule is no production queries without explicit owner go-ahead. (d) otherwise Phase 4 re-inflates the orchestrator with everything fan-out was meant to keep out — that, not Phase 2/3, is where the context ceiling actually binds.

[D-013] 2026-07-29 (orchestrator) — Phase 1 trimmed from 3 research workers to 2; all four brief-named topics still covered. — Checker LF-4, accepted in part only. The checker argued for a single worker on the grounds that Phase 4 already prescribes the structure (≤150-line index, topic files, CONTRIBUTING, GAPS), making "recommend ONE structure" largely a ratification of the brief. That is half right — but the brief explicitly commissions this research and defines checkpoint 1 as "confirm recommended structure", so cutting it to one worker would under-deliver against an explicit instruction. Two workers: W1 = entry-point/load-protocol conventions (CLAUDE.md, AGENTS.md, llms.txt, memory-bank, progressive disclosure); W2 = per-fact provenance/staleness/versioning conventions in semantic layers and metric stores — the genuinely open design question. Web research triggers the `save-web-research` obligation; orchestrator discharges it centrally, workers do not write.

[D-015] 2026-07-29 (owner @ checkpoint 1) — All four CP1 items ratified as recommended. (1) Structure = progressive disclosure + addressable fact rows: `CONTEXT.md` (≤150L always-on index) + business/metrics/data-model/dashboards/GAPS/CONTRIBUTING topic files; row schema `id | statement | source_ref | source_updated_at | last_verified | confidence | aliases | note`. (2) Core metric scope = the 11 v1 metrics + North Star get FULL treatment; the remaining ~53 catalog metrics get an index-only row + one GAPS row each. (3) Add `KB-POINTER.md` stub in the session-folder `ptl-selfserve/` (new file) so the KB is discoverable from the wrong-but-obvious path. (4) Precedence ladder confirmed as D-009: observed card SQL > DECISION_LOG D1–D7 > iteration-1 catalog > journey/proposal docs > Notion review, with SQL-vs-D-ruling contradictions never auto-resolved. — Owner-selected at the checkpoint gate. Item (2) bounds Phase 3 extraction cost while keeping coverage gaps explicit rather than hidden; item (3) makes the DONE-WHEN acceptance test reachable without prior knowledge of the canonical path; item (4) confirms the D-004 reading, closing the checker's LF-3 objection that the orchestrator had overridden an owner rule unilaterally.

[D-016] 2026-07-29 (orchestrator) — Phase 2 pinned commit SHAs for all provenance: Desktop clone (PTL) = `7a43470` on `claude/ptl-metric-catalog-map`; dev clone (PnM reference) = `851886f` on `claude/pnm-p80-orderedits`. Both remote `github.com/akshayjain00/selfserve_data`. — D-008 mandates `repo@<sha>:<path>#L<n>` citations. The SHAs must be fixed BEFORE fan-out, otherwise workers cite coordinates that shift under them and the immutability the format exists to provide is lost.

[D-017] 2026-07-29 (orchestrator) — Two standing UNCONFIRMED assumptions are now RESOLVED to `verified` from rank-1 evidence (observed card SQL). (a) `orders.state` enum: `0=Open, 1=Assigned, 2=Picked_up, 3=Completed, 4=Cancelled`, from card 33519's CASE mapping — retires the `STATE_ENUM_CONFIRMED=False` flag carried through every prior artifact, and adds states 0/1/2 which were undocumented. (b) Business-customer rule: `CASE WHEN c.frequency IN (1,2,3,4) THEN 'Business' ELSE 'Personal' END` against `prod_curated.oms_public.customers`, joined `customer_mobile = c.mobile` — used IDENTICALLY by every segmenting card on dashboard 4569, retiring the prototype's unconfirmed `frequency IN (1,2,3,4)` filter and settling the `oms_public.customers` vs `dim_customers` question in favour of `oms_public` FOR THAT DASHBOARD. — Both were blocking flags on stakeholder-readiness. Residual caveats recorded, not glossed: the state enum comes from a human-authored CASE in a card, not a warehouse data dictionary, so it is "verified via card SQL", not "verified via source of truth"; and dashboard 4198 DOES reference `prod_eldoria.core.dim_customers`, so the two dashboards disagree on customer source — that cross-dashboard split is a new GAPS row, not a resolution.

[D-018] 2026-07-29 (orchestrator) — CBDF and CADF remain `[unverified]`; all four conflicting positions recorded, none selected. — Dashboard 4198 was the designated grounding surface for cancellation metrics, and SQL inspection of all 11 cancellation cards shows a flat `orders.state = 4` with NO join to `ORDER_VEHICLES` and no assignment signal — there is no before/after-driver-found split on that dashboard at all. Positions: (i) the Notion review reports CBDF and CADF as real Apr-26 numbers; (ii) the prototype computes them as `state=4 AND vehicle_assigned=0/1`, self-described as "49366-style"; (iii) ruling D5 names dashboard 4793 canonical — a dashboard OUTSIDE the three surfaces this task scoped; (iv) dashboard 4198 has neither. Picking any one would be exactly the silent resolution the brief forbids. Verifying properly requires dashboard 4793, which is out of scope — recorded as a GAPS row with the precise next action rather than quietly widening scope.

[D-019] 2026-07-29 (orchestrator) — The North Star Metric remains `[unverified]` — it has an owner ruling and a reported value but NO implementation anywhere in scope. — D4 defines NSM as "Monthly Transacting Business Customers on PTL"; the Notion review reports 2247 (Apr-26) / 1879 (Mar-26); the prototype registry marks `nsm_txn_business_customers` as `"authored"` and emits no column; and no card on dashboard 4569 computes it. Closest verified analogs are cards 38287 / 39117, `COUNT(DISTINCT customer_mobile)` over completed (`state=3`) orders per period — recorded as the named closest analog, NOT as the definition. The reported figure cannot be reproduced from any in-scope SQL, and the KB must say so plainly rather than let an owner ruling plus a plausible number imply a verified metric.

[D-020] 2026-07-29 (orchestrator) — Coverage is stated explicitly, never implied. 83 cards were NOT opened (55 on dashboard 4198 of ~82 unique; 28 on dashboard 4569 of 50) and each is listed with its reason in the extracts and in GAPS.md. — Both dashboards are far larger than the brief implied, and the D-015 scope ruling bounds full treatment to the 11 v1 metrics + NSM. An unstated cap reads as full coverage and is the failure mode that makes a KB confidently wrong; naming the uncovered surface converts an invisible blind spot into a tractable backlog.

[D-021] 2026-07-29 (orchestrator) — BLIND checker gate run on the finished KB (Opus; given the artifact + the sources, NOT the author's reasoning). Verdict SHIP WITH FIXES; all must-fixes folded in. — The KB is validated against a spec (DECISION_LOG D1–D7 + observed card SQL), which is exactly the case the skill reserves the blind variant for. It found four FALSE CLAIMS I introduced, and three of the four share one root cause worth recording: **I hardened a worker's narrow finding into a broad absolute during synthesis.** (C-2) a worker reported "no card computes NSM as a dedicated metric"; I wrote "computed nowhere" — card 39117 in fact computes `COUNT(DISTINCT customer_mobile)` with `category='Business'`, `state='3'`, month grain, internal users excluded, which IS ruling D4's definition. (C-3) a worker reported the CBDF/CADF cards "carry no `is_test` binding"; I wrote that they lack internal-user exclusion — they exclude it hardcoded via `NOT IN (SELECT mobile FROM ptl_internal_users)`, so acting on my version would have double-excluded. (C-1) I labelled the catalog tally "15 confirmed / 6 unverified / 64 never-opened" when DECISION_LOG Drift 2 states "15 confirmed / **6 contradicted** / **64 unverified**" — I inverted the risk ranking, since `contradicted` is the highest-risk bucket. (C-4) I cited card 33483 as M-002's source where catalog #19 and `metrics_registry.py:59` both say 33462 — a silent lineage re-point, the exact thing the KB's own rules forbid. Also folded: CADF delta corrected 1.2pp → 0.82pp (13.81−12.99) — a leadership-note-grade arithmetic error; unopened-card headline corrected 83 → 93 (the 4793 group was enumerated but omitted from the total); cross-reference range `G-090…G-101` → `G-120…G-131`; `G-005a`/`G-005b` renumbered to `G-114`/`G-115` to obey the KB's own ID schema; six violations of the KB's own Rule 6 (values inlined into definition rows) removed; ~31 provenance-format violations of its own §3 corrected; the invented ruling id "D-015" removed (that is an orchestration decision in THIS file, not a DECISION_LOG domain ruling — citing it inside the KB conflated the two logs); the unsourced "39/39 harness" claim and the monotonically-drifting "93,521 views" fact removed. Confidence recalibrated: business-context rows sourced only to the Notion review or the ProdOps instruction file were marked `verified`, which the KB's own §4 bar (SQL, code, or explicit owner ruling) does not permit — all downgraded to `unverified`, with a header note explaining that the confidence scale grades EVIDENCE TYPE, so doc-sourced business context can never read `verified` and that is the correct signal rather than a defect. Two rulings were also misrepresented and are fixed: D3 (offline both-bases) does not enumerate retention, so only `trips_sql` is a genuine D3 breach — one builder, not two; and D6's explicit carve-out exempting Business Session Conversion (#14) from both-bases was dropped entirely, which would have invited a future session to "fix" correct code.

[D-022] 2026-07-29 (orchestrator) — ACCEPTANCE TEST (zero-context, Sonnet, prompt = the CONTEXT.md path + four questions and nothing else) PASSED, self-rated 5/5. — This is the D-010 gate that tests the actual DONE-WHEN criterion (loadability), which the blind checker does NOT test (it tests accuracy). The agent correctly described PTL and the self-serve scope, gave the Total Fulfilment and CBDF formulas with their caveats, and — the deliberate trap — when asked for the North Star's value answered "I cannot confidently state how the reported NSM number was actually produced" instead of reporting 2,247 as fact. That is the single most important result of the whole build: the KB transmits calibrated uncertainty, not just correct facts. It also confirmed the load protocol works — it reached the answers by opening only `metrics.md` and `GAPS.md`, exactly as the routing table predicts, without needing `dashboards.md`, `data-model.md` or the raw sources.

[D-023] 2026-07-29 (orchestrator) — Staleness fingerprints are recorded ONLY for cards read directly (33519, the 4793 family, plus 39117 and 49366 supplied by the checker). The ~20 cards on dashboards 4198/4569 that feed KB facts have NO `source_updated_at` and the gap is logged as `G-116`, not silently left blank. — The checker correctly identified that the staleness mechanism — the KB's main defence against rot — is unusable for the two largest surfaces without these timestamps. Fetching them means ~20 `get_card` calls whose payloads embed full SQL, which would consume the context needed to finish the fix pass. Recording the gap with the exact closing action (one `get_card` per listed card, read `updated_at` only) is honest and cheap; silently leaving the column blank would let a future session believe the check had been done.

[D-024] 2026-07-29 (orchestrator) — Requirement-coverage audit found FOUR task-named sources never read; all now read via worker P2e. — Root cause worth recording: I built the Phase-2 worker's source list from **what I found on disk**, not from the **task's Context block**, so a filesystem scan silently replaced the specification. The dev clone's `pnm-selfserve/` looked like "the PnM material" and was accepted as such without diffing it against the five files the brief actually named. Missed: `ProdOps/selfserve/pnm/queries.py` (387L, the MBR automation query layer — I had read the PnM *self-serve prototype* instead, a different artifact), `rendered_tpo_202605.sql` (106L), `iteration2readinessledger.md` (a diverged 124-line copy vs the 151-line one read), and the **Metric Store POV PDF** (27.5 MB) named explicitly in the brief. Nominal-only: `iteration1metriccatalogandarchitecture.md` proved byte-identical to the copy already read. Readiness-ledger divergence resolved benignly — the 151-line dev copy is the NEWER one (it appends a post-review update), so the version already read was the correct one. Lesson for any future inventory phase: enumerate sources from the specification first, then map them to disk — never the reverse.

[D-025] 2026-07-29 (orchestrator) — Record the Argus-vs-D2 architectural conflict in the KB as `G-132`, without resolving it. — The Metric Store POV ("Porter Product Argus") **evaluated and rejected**, for its own programme, a "per-metric SQL template file" approach — hand-rolled, no semantic layer — in favour of dbt-authored, PR-gated definitions. PTL's ruling D2 (raw `partload_application` + hand-rolled registry, governed layer deferred) is structurally that rejected shape. The POV names no vertical, so **nothing binds PTL today** and calling this a violation would overstate it. But PnM is hitting the identical fork live under a standing "no dbt model → not eligible for the metric store" rule, so the choice is arriving. A KB that documents D2 while omitting that a company-wide programme rejected its shape would be hiding a strategic conflict from exactly the future session most likely to build on D2. Also captured: Argus requires a **named owner per metric** (this KB records none → `G-133`) and a value+freshness+lineage+confidence **trust footer** (partly met → `G-134`); and its three-tier confidence model plus refuse-rather-than-guess rule are independently convergent with the scheme this KB had already adopted. One fact upgraded: aggregate-then-ratio and `NULLIF(denominator,0)`→NULL are confirmed **Porter-wide** rather than PTL-local, since PnM's automation derives them independently (`B-074`).

[D-026] 2026-07-29 (orchestrator) — CONTRIBUTING §2's row schema amended to match what the KB actually does, rather than forcing the files to a schema that does not fit them. — The audit found CONTRIBUTING mandating `source_updated_at` and `last_verified` as per-row REQUIRED columns while `last_verified` appeared as a column in zero files (it is a file-level declaration), `source_updated_at` appeared only in `data-model.md`, `business.md` carried three different table shapes, and `metrics.md` used per-metric blocks rather than rows at all. A future session following the protocol would have emitted rows incompatible with every existing one — a self-inflicted conflicting specification. Resolution: four columns are the contract (`id`, `statement`, `source_ref`, `confidence`); `source_updated_at` is required only where the source HAS such a stamp (Metabase cards do, documents do not); `last_verified` is declared once per file with per-row inline override, since repeating an identical date on every row would triple table width for no information; and tables may carry only the columns that apply, with `metrics.md`'s block form explicitly sanctioned because a formula plus caveats plus multiple sources does not fit one line. The brief's requirement — every fact carries source link, last-verified date, and confidence — is still satisfied; a file-level date is a claim about every row in that file. Also fixed: `B-055` pointed at `G-116` (staleness fingerprints) when it is an acronym-expansion gap — a reference that resolved cleanly and was still semantically wrong, which is a class of error no existence-check can catch.

[D-027] 2026-07-30 (orchestrator, owner-authorised) — FIRST PRODUCTION QUERY of this workstream, executed with explicit owner go-ahead: `execute_card(39117)` bounded to `start_date=2026-03-01`, `end_date=2026-04-30`, `frequency=Month`. Result Mar-26 = **1879**, Apr-26 = **2247** — an EXACT match to the reported NSM. `G-003` CLOSED; `M-001` promoted to `verified`. — The standing rule is no production query without explicit owner go-ahead; it was given. The lowest-risk form was chosen deliberately: executing an existing saved card rather than authoring SQL, which also honours the locked architecture principle that the AI never authors SQL. **The exact match is the important part, and it cuts both ways** — because the card reproduces the leadership figure precisely, the leadership figure inherits the card's defects. Two are material and are now `G-141` and `G-142`: (1) the internal-user exclusion is applied to the ONLINE leg only — the offline gsheet CTE has no `ptl_internal_users` filter, so the NSM shown to leadership is inflated by internal offline orders by an unquantified amount; (2) the offline `UNION` is hardcoded into the CTE structure with no parameter or optional block, so ruling **D3's dual-base requirement cannot be satisfied from this card at all** without forking the query. A third, `G-143`, is an analysis trap rather than a defect: the retained/reactivated columns compute against prior periods inside the query window, so Mar-26 reported 0 retained purely because the window began 2026-03-01 — only `ACTIVE_CUSTOMERS` is safe to read from a narrow window. Also recorded: the card's stored date defaults are 2025-05-01/2025-08-31, so running it unmodified returns four 2025 months rather than the current one.

[D-028] 2026-07-30 (orchestrator) — Closed the blind checker's OWN coverage gap by independently re-verifying every 4198/4569 card claim (2 Sonnet workers, metadata reads only). Four KB claims REFUTED or softened; two gaps closed; six new defects found. — The blind checker had declared it never opened dashboards 4198/4569 for context-budget reasons, which left `G-002`, `G-004`, `G-011`, `G-022` and M-003's `verified` resting on my single reading — reviewed once, not twice. Nothing structural prevented closing it; the owner asked why not, and the answer was only cost. Refuted/softened: 33485/37419 are NOT byte-identical (same formula, different SQL text and display type); "5 metrics each" holds for 33466/43238 but 37104 returns 3 (split by EDD); card 39104 does not "ignore" `{{frequency}}` — it has no such template tag at all, so it cannot ignore one; and the `<60s` denominator placement is now confirmed verbatim rather than asserted. Closed: `G-007` — card 33706 uses `updated_at`, so the catalog was right and the prototype is wrong; `G-118` — card 33483 has NO state predicate anywhere in its SQL and is architecturally incapable of producing a completed-orders figure under any parameterisation, making 33462 canonical beyond doubt. New: `G-135` (AOV splits on date basis too, so 3 revenue bases × 2 date bases), `G-136` (**`database_id` is not uniform — metric cards are db73 while card 33519 is db83, and prior artifacts cite db108; `T-001` and the unit scalings were therefore verified on a different database than the metrics query**), `G-137` (cards 47540/48449 expose Start/End Date parameters that are inert against hardcoded `pickup_date >= '2026-02-01'`, 48449 additionally hardcodes `pickup_city IN ('Bangalore','Mumbai')` while named "City Wise", and 49365 hardcodes a `>= '2026-03-01'` floor so earlier dates silently return nothing rather than erroring — a user filters, sees plausible output, and is wrong), `G-138` (`{{frequency}}` means cohort-lag granularity on 43406 but the customer-tier column elsewhere), `G-139` (card 52889 sits in a different collection from its family), `G-140` (the repeat flag on 39107/39149 fires in the acquisition period itself and never resets, saturating toward ~100%).

[D-029] 2026-07-30 (orchestrator) — `G-116` CLOSED: all 29 cards the KB depends on now carry a `source_updated_at`, tabulated in `dashboards.md`. — The staleness mechanism is the KB's principal defence against rot and was inactive for its two largest surfaces. Closing it was cheap and mechanical — `updated_at` came back on the same `get_card` calls the verification pass in D-028 already needed, so the two tasks were merged into one sweep rather than run separately. The sweep also surfaced `G-136` (non-uniform `database_id`), which no amount of reading SQL would have revealed — it is card metadata, not query text.

[D-030] 2026-07-30 (orchestrator, owner-requested) — `G-136` investigated and downgraded high→low; `T-001` split into `T-001`/`T-001a`; `T-010`/`T-011` re-verified on db73; `T-012` demoted to `unverified`. — The three Metabase ids resolve to distinct CONNECTION PROFILES, all `engine: snowflake`: db73 `SNOWFLAKE_NEW_INI` (every metric card), db83 `SNOWFLAKE_BUSINESS_ENGG_PRODUCT` (card 33519 only), db108 `SNOWFLAKE_NI_ELDORIA` (the governed dbt layer D2 defers to). The decisive evidence that they address the same objects is that db73 cards reference the **identical fully-qualified tables** db83's card 33519 uses — `partload_application.orders`, `.order_fares`, `.quotations`, `partload_analytics.ptl_internal_users` — which makes "different roles/warehouses over one account" overwhelmingly the better reading than "different data". Rather than accept that inference for everything, each affected fact was re-checked directly on db73: `state=3` Completed and `state=4` Cancelled are confirmed across 8+ db73 cards; `estimated_fare/100` is confirmed on card 33706; `total_fare/100` on cards 37413 and 52889. Two facts did NOT survive re-verification and were demoted rather than left implicitly safe: the `0=Open, 1=Assigned, 2=Picked_up` labels appear on no db73 card (33462, the only db73 card touching those states, groups 0/1/2 as an unnamed "in-process" bucket), so they were **split out as `T-001a`, `unverified`** — splitting rather than downgrading the whole row keeps the well-evidenced half (3/4) at full strength; and `chargeable_weight/1000` is referenced by **no db73 card at all**, so `T-012` is now `unverified`. Independent corroboration recorded as `T-013`: db73 card 52889 uses a column literally named `discount_amount_minor_units`, and "minor units" is the standard term for paise — the currency convention is confirmed lexically as well as arithmetically. Separately, `G-018` was strengthened: the partition-pruning anti-pattern is not confined to one optional filter on 33519 — card **33706, a live db73 revenue/AOV card, uses `date(updated_at + interval '330 mins')` as its PRIMARY date predicate**, which defeats micro-partition pruning and forces a full scan, making this a cost/latency issue rather than a style note.

[D-031] 2026-07-30 (owner) — On `G-137` (inert filters) and `G-018` (pruning anti-pattern): do BOTH — escalate to the card owners AND keep the KB warnings standing. Owner sends the message; orchestrator drafted it. — Escalate-only would leave the KB silently trusting cards that are still broken in the interim; warn-only would leave a defect live in production indefinitely. Doing both is the correct pairing, and the two issues were drafted as ONE message because they almost certainly share owners — 33706 appears in both (inert-filter family by dashboard, anti-pattern by predicate). Recorded in the gap rows as `ESCALATED` rather than closed, with an explicit instruction: **do not delete these rows when the cards are fixed — close them with the date and the fixing card version.** A deleted gap row loses the fact that the trap ever existed, which is exactly the institutional memory a future session needs when a number looks wrong. The drafted message deliberately leads with the filter bugs over the performance ones, since a silently-wrong answer outranks a slow one. Card `creator` metadata was NOT looked up unprompted — routing a bug report to a named colleague is legitimate, but it is personal data and the owner should ask for it rather than have it volunteered.

[D-032] 2026-07-30 (owner) — Owner authorized executing existing saved Metabase cards for validation ("skip the rule not run query"), extending Phase 4 to attempt closing the 64 catalogue-`unverified` metrics. — Orchestrator broke the 64 down by what actually blocks each, rather than treating the query-execution rule as the only constraint: **~9 have a named Metabase card** (checkable by the same read+execute method already used for the North Star, fully within the locked "AI never authors SQL" architecture); **~8 live in Amplitude** (a different system — probed `get_amplitude_context` and confirmed access, unrelated to the Snowflake query rule); **2 have no card in either system** (#9, #18) and **~24 are labeled "Sheets + Others"** with no confirmed card (ambiguous — may overlap with the 93 already-catalogued unopened dashboard cards); **~21 live in Freshdesk/DataDog/Survey/Finance**, systems with no connector in this session — genuinely blocked on tooling, not on the query rule, and "skip the rule" cannot unblock a connector that doesn't exist. The owner's permission is read as covering **execution of existing cards**, not a lift of the separate, more fundamental "AI never authors SQL" architecture principle — the two are distinct and conflating them would let a scope-hygiene rule's relaxation quietly relax an architectural one it was never meant to touch. 2 metrics (#42, #45) closed immediately at zero additional cost by re-reading Phase-3 extracts already in hand → `M-012`, `M-013`. Remaining ~9 Metabase-carded and ~8 Amplitude metrics dispatched to parallel workers. The ~24 ambiguous and ~21 tool-blocked metrics were NOT silently attempted or silently dropped — surfaced to the owner as a scoped question rather than either extreme.

[D-033] 2026-07-30 (orchestrator) — Results of the D-032 validation batch: 2 metrics closed free, 3 Amplitude metrics verified (incl. resolving `VSS`), 3 found genuinely stale/missing, 7 blocked on a Metabase auth error mid-task. — Per the mandatory MCP auth protocol, the Metabase batch worker hit a token-expired error on its first call, confirmed it on a second card, found no `authenticate` tool for the domestic connector in this session's tool list, and stopped rather than retrying or working around it — the correct response to an auth error is a hard stop, not debugging. I attempted the same authenticate tool myself and confirmed it does not exist for this connector (only `Metabase - INT` exposes one); this is a genuine tool-availability gap, not user error, and is now `G-144`, requiring the owner to reconnect that connector before cards 48923/44469/48922/48984/48919/49311 can be read. The Amplitude batch had no such issue (auth probed clean beforehand) and returned real, mixed findings: #4/#7/#8 confirmed matching their catalogue definitions (verified); #18's only candidate chart ends at "book now clicked" rather than "order placed" — a genuine definition mismatch, not confirmed as a match; #5/#6 use numeric chart ids that don't resolve at all, most likely stale references from before Porter's 2026-01-01 Mixpanel→Amplitude migration (`G-145`); #3 has no matching chart anywhere in 100 search results and was not substituted (`G-146`). The single most valuable side-effect: `VSS` — used undefined throughout every PTL source since Phase 2 — is now confirmed as "Vehicle Selection Screen" from literal Amplitude event names, and `FTL` is confirmed to be a title label over an enumerated vehicle_id list rather than a literal taxonomy value. `business.md` and `GAPS.md` updated accordingly; `metrics.md`/`CONTEXT.md` totals corrected from 11/74 to 13/72 to reflect the two free promotions (`M-012`, `M-013`) — the walkthrough artifact built earlier this session still shows the pre-promotion 11/74 figures and needs a refresh pass before it is treated as current.

[D-034] 2026-07-30 (orchestrator) — Metabase reconnected without owner intervention mid-session; both remaining batches (7 blocked cards + 24 Sheets-bucket search) completed. 9 more metrics promoted, 2 confirmed as catalogue mapping errors, 1 major structural finding surfaced. — Probed card 48923 directly and it succeeded, so re-dispatched both queued workers rather than waiting on the owner. Net across the full 2026-07-30 validation push (D-032/D-033/D-034 combined): **20 of 85 catalogue metrics now fully written up** (11 v1 + 9 new: `M-012`–`M-020`), up from 11 at the start of the day. Two `unverified` rows turned out to be **catalogue errors, not just unchecked**: #16/#17 diverge on customer-source table (`dim_customers` vs the canonical `oms_public.customers`) and #17's numerator is a raw click event never joined to order completion (`G-148`); #44's assigned card computes booking-time latency in minutes, not an inter-order-day interval — the catalogue's own card mapping is wrong (`G-149`). The single highest-leverage finding: **12 metrics (owner/vehicle-level supply health) may be structurally untrackable** — every Supply-tab card operates at vendor (fleet-company) grain, not individual owner/vehicle grain, which is an entity-model gap in the underlying instrumentation, not a missing-card problem (`G-151`) — this is a planning question for the metric owner, and no further KB search effort should be spent on these 12 until that's answered. 6 metrics confirmed genuinely absent after a real search, with the negative evidence recorded rather than left as a bare "unverified" (`G-150`). Totals corrected again: metrics.md/CONTEXT.md now read 20 full / 65 index-only (was 13/72 after the morning's two free promotions, 11/74 before that). 9 newly-found cards lack a staleness fingerprint (`G-152`) — the search worker was scoped to definitions, not timestamps; recorded rather than fabricated. The walkthrough artifact and its companion WALKTHROUGH.md are now further stale (still show the original 11/74) and need a dedicated refresh pass before being treated as current — not done in this same session given the scale already covered.

[D-014] 2026-07-29 (orchestrator) — Notion "PTL Product Ops Review, May '26" (page `3449c6eaaa6d8036bb51d679b6182767`) added as a Phase 2 source for metrics, definitions AND actual numbers. Numbers enter the KB ONLY as a dated snapshot labelled `as-of May '26`, never as living facts. — Owner instruction mid-turn. This is the review whose manual production is being automated, so it is authoritative for *which* metrics matter and what is narrative vs numeric. But a KB asserting a bare metric value goes silently wrong the moment the period rolls; definitions are durable, values are point-in-time. Snapshot values are useful as reconciliation anchors (do our rendered numbers match the review?) and are stored as such, in their own section, never inlined into a definition row. Orchestrator resolves and reads this page centrally (checker BS-6) rather than letting workers fuzzy-search for it.
