# ➜ The PTL self-serve knowledge base is NOT in this folder

This directory holds only the kickoff prompt and the coordination record.
The knowledge base — and every asset it describes — lives in the git repo:

```
~/Desktop/AI_V2/ProdOps/selfserve/pnm/selfserve_data/ptl-selfserve/kb/
   repo:   github.com/akshayjain00/selfserve_data
   branch: claude/ptl-metric-catalog-map
```

**Start with `kb/CONTEXT.md`.** It is the entry point and tells you what to load next.

> Why the split: at least three directories on this machine are named `ptl-selfserve`. The KB sits
> beside the assets it cites (`DECISION_LOG.md`, `iteration-1-ptl-metric-catalog.md`,
> `selfserve_nlq/`) so its relative links resolve and it is version-controlled with them.
> Build record for the KB itself: `coordination/kb-build/` in this folder.
